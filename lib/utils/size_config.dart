class SizeConfig {
  static const double desktopWidth = 1200;
  static const double tabletWidth = 800;

  /*static late double screenWidth, screenHeight; // work with Mobile and Tablet only!
  static init(BuildContext context){ // call it in first view (ex. splash screen)
    screenWidth = MediaQuery.of(context).size.width;
    screenHeight = MediaQuery.of(context).size.height;
  }*/
}